export type RawGameData = Record<string, unknown> & { activePlayer?: Record<string, unknown>; allPlayers?: Array<Record<string, unknown>>; gameData?: Record<string, unknown>; events?: { Events?: Array<Record<string, unknown>> } };
export type NormalizedGameData = { gameId:string;gameTime:number;gameMode:string;mapName:string;activePlayer:{summonerName:string;championName:string;level:number;team:string};players:Array<{summonerName:string;championName:string;team:string;isActivePlayer:boolean;summonerSpells:{spell1:string;spell2:string};keystone:string;runes:string[];level:number;items:Array<{id:number;name:string}>}>;events:Array<{name:string;time:number}>;updatedAt:string };
